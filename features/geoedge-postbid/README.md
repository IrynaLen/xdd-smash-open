# geoedge-postbid

Wraps HTML creatives with the Geoedge ad-quality tag.

- Stage: `postbid-ssp`
- Namespace: `geoedge`

Gated on `creative.isHtml`, so video and native pass through untouched. Included
as a worked example of creative wrapping as much as for its own sake.
